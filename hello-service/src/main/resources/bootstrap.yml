spring:
  application:
    name: sample
  cloud:
    config:
      uri: http://configserver:8888
      profile: dev
      name: didispace
#      对应config-server中的用户名和密码
      username: admin
      password: admin
#      fail-fast: true
#      retry:
#        initial-interval: 2000
#        max-interval: 10000
#        multiplier: 2
#        max-attempts: 10